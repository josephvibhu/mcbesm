v1.0

1. Modified from mcbeup.sh script to fit the need

v2.0 current

1. Added auto config preset to modify server.properties file
1.1. set view-distance=64
1.2. set max-threads=0 # 0 uses all threads
1.3. modifies the server port to the one set by playit.gg

cons

1. if else block for server.properties not found condition solution
